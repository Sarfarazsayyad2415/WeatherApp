# WeatherApp
A simple Python weather application that fetches real-time weather data using the Requests library and parses JSON responses from a weather API. Displays current temperature, humidity, weather conditions, and more through a clean command-line interface.
